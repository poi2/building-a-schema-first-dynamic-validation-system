version: v2
modules:
  - path: proto
deps:
  - buf.build/bufbuild/protovalidate
  - buf.build/googleapis/googleapis
lint:
  use:
    - STANDARD
  except:
    - FIELD_LOWER_SNAKE_CASE  # Allow _plan for context enrichment
breaking:
  use:
    - FILE
