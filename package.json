{
  "name": "celo",
  "version": "0.1.0",
  "private": true,
  "description": "Schema-First Dynamic Validation System PoC",
  "workspaces": [
    "services/bff",
    "services/fe",
    "tests/e2e"
  ],
  "scripts": {
    "lint:md": "markdownlint \"**/*.md\" --ignore node_modules",
    "lint:md:fix": "markdownlint \"**/*.md\" --fix --ignore node_modules"
  },
  "devDependencies": {
    "markdownlint-cli": "^0.39.0"
  }
}
